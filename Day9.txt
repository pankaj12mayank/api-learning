Great 🔥 अब तुम momentum में हो — यही consistency तुम्हें pro बनाएगी।

---

# 🚀 Day 9: Dependency Injection (Depends) + Clean DB Handling

आज हम सीखेंगे:

1. ❓ Problem क्या था Day 8 में
2. 🔧 Dependency Injection क्या होता है
3. 🧠 FastAPI में `Depends()` कैसे काम करता है
4. ⚡ Production-level DB session handling
5. 🏗️ Upgrade your project (clean architecture)

---

# ❗ Day 8 Problem (Very Important समझना)

तुमने ये किया था:

```python
db = SessionLocal()
```

👉 Problem:

* हर function में manually DB create + close
* भूल गए close करना → memory leak 😨
* Not scalable

---

# 💡 Solution = Dependency Injection

👉 Simple भाषा में:

**Dependency Injection = “जो चीज बार-बार चाहिए, उसे automatically inject कर दो”**

---

# 🧠 Real-Life Example

सोचो:

* तुम हर बार चाय बनाने के लिए पानी खुद लाते हो ❌
* या कोई तुम्हें ready पानी दे देता है ✅

👉 `Depends()` वही करता है

---

# ⚙️ Step 1: Create DB Dependency

📄 `database/db.py`

```python
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker, declarative_base

DATABASE_URL = "sqlite:///./test.db"

engine = create_engine(DATABASE_URL)

SessionLocal = sessionmaker(bind=engine)

Base = declarative_base()

# 🔥 NEW CODE
def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```

---

# 🧠 ये `yield` क्यों?

👉 Flow समझो:

```text
Request आता है
    ↓
get_db() चलता है → db create
    ↓
API function को db मिलता है
    ↓
Response return
    ↓
finally → db.close()
```

👉 मतलब:

* Auto open
* Auto close
  🔥 Production-ready

---

# ⚙️ Step 2: Use Depends in Routes

📄 `routes/product_routes.py`

```python
from fastapi import APIRouter, Depends
from sqlalchemy.orm import Session

from database.db import get_db
from schemas.product_schema import ProductCreate, ProductResponse
from services.product_service import *

router = APIRouter()

@router.post("/products", response_model=ProductResponse)
def create_product(product: ProductCreate, db: Session = Depends(get_db)):
    return create_product_service(product, db)


@router.get("/products", response_model=list[ProductResponse])
def get_products(db: Session = Depends(get_db)):
    return get_products_service(db)
```

---

# ⚙️ Step 3: Update Service Layer

📄 `services/product_service.py`

```python
from models.product_model import ProductDB

def create_product_service(product, db):
    new_product = ProductDB(
        name=product.name,
        price=product.price,
        qty=product.qty
    )

    db.add(new_product)
    db.commit()
    db.refresh(new_product)

    return new_product


def get_products_service(db):
    return db.query(ProductDB).all()
```

---

# 🔥 अब क्या बदला?

| पहले ❌             | अब ✅           |
| ------------------ | -------------- |
| db manually create | db auto inject |
| close भूल सकते थे  | auto close     |
| messy code         | clean code     |

---

# 🧠 Deep Concept (Interview Level)

👉 `Depends(get_db)` क्या करता है?

* FastAPI internally:

  * function call करता है
  * result inject करता है
  * lifecycle manage करता है

👉 इसे कहते हैं:
➡ **Inversion of Control (IoC)**

---

# 🧪 Practice (Upgrade Your Project)

अब ये add करो 👇

---

## ✅ 1. Get Product by ID

```python
@router.get("/products/{id}", response_model=ProductResponse)
def get_product(id: int, db: Session = Depends(get_db)):
    return get_product_service(id, db)
```

---

## ✅ 2. Update Product

```python
@router.put("/products/{id}", response_model=ProductResponse)
def update_product(id: int, product: ProductCreate, db: Session = Depends(get_db)):
    return update_product_service(id, product, db)
```

---

## ✅ 3. Delete Product

```python
@router.delete("/products/{id}")
def delete_product(id: int, db: Session = Depends(get_db)):
    return delete_product_service(id, db)
```

---

# 🧠 Real World Insight

👉 हर बड़े backend में ये होता है:

* DB connection pool
* Dependency injection
* Clean architecture

👉 तुम अब junior dev level cross कर रहे हो 🚀

---

# 🎯 Day 9 Completion Checklist

✔ Depends समझ आया
✔ yield concept clear
✔ DB lifecycle समझा
✔ Code clean किया
✔ CRUD working with DI

---

# 🧠 Homework (Important)

👉 Add:

* Proper error handling (404 if not found)
* Try/except in service layer

---

# 🚀 Day 10 Preview

👉 हम सीखेंगे:

* Exception Handling (HTTPException)
* Custom error messages
* Clean API responses (industry standard)

---

अगर तुम चाहो:
👉 मैं तुम्हारा पूरा project review कर सकता हूँ
👉 या तुम्हें GitHub-ready production structure दे सकता हूँ

बस बोलो: **"Review my project"** 🔍
