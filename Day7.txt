Perfect 🔥
Now you’re entering **real backend development** 🚀
👉 Day 7 = **Database Integration (SQLite)**

---

# 📅 **Day 7 – Connect Real Database (SQLite)**

Until now:

* ❌ Data lost on restart (list used)
* ❌ Not real-world

Today:

* ✅ Persistent data
* ✅ Real database
* ✅ Industry-level backend

---

# 🧠 What You’ll Learn Today

* What is a database
* Connect DB with FastAPI
* Use ORM (**SQLAlchemy**)
* Store & fetch real data

---

# 🧱 Step 1: Install Required Packages

Run this:

```bash
pip install sqlalchemy
```

---

# 🧱 Step 2: Create Database File

📁 Project structure:

```
project/
 ├── main.py
 ├── database/
 │    └── db.py
 ├── models/
 │    └── product_model.py
 ├── services/
 ├── routes/
```

---

# 🧱 Step 3: Create DB Connection

📁 `database/db.py`

```python
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker, declarative_base

DATABASE_URL = "sqlite:///./products.db"

engine = create_engine(DATABASE_URL, connect_args={"check_same_thread": False})

SessionLocal = sessionmaker(bind=engine)

Base = declarative_base()
```

---

# 🧠 Concept

| Term    | Meaning       |
| ------- | ------------- |
| engine  | DB connection |
| Session | Talk to DB    |
| Base    | Model parent  |

---

# 🧱 Step 4: Create DB Model (IMPORTANT)

📁 `models/product_model.py`

👉 Replace Pydantic model with **SQLAlchemy model**

```python
from sqlalchemy import Column, Integer, String
from database.db import Base

class Product(Base):
    __tablename__ = "products"

    id = Column(Integer, primary_key=True, index=True)
    pname = Column(String)
    price = Column(Integer)
    qty = Column(Integer)
    meg = Column(String)
```

---

# 🧠 Important

👉 This is **table schema**, not API model

---

# 🧱 Step 5: Create Table

📁 `main.py`

```python
from fastapi import FastAPI
from database.db import engine, Base
from routes.product_routes import router

app = FastAPI()

Base.metadata.create_all(bind=engine)   # ✅ create table

app.include_router(router)
```

---

# 🧱 Step 6: Update Service Layer (REAL DB)

📁 `services/product_service.py`

```python
from database.db import SessionLocal
from models.product_model import Product

# CREATE
def create_product_service(product):
    db = SessionLocal()

    new_product = Product(
        id=product.id,
        pname=product.pname,
        price=product.price,
        qty=product.qty,
        meg=product.meg
    )

    db.add(new_product)
    db.commit()
    db.refresh(new_product)
    db.close()

    return new_product


# READ ALL
def get_all_products_service():
    db = SessionLocal()
    products = db.query(Product).all()
    db.close()
    return products


# READ ONE
def get_product_service(product_id):
    db = SessionLocal()
    product = db.query(Product).filter(Product.id == product_id).first()
    db.close()
    return product


# UPDATE
def update_product_service(product_id, updated_product):
    db = SessionLocal()
    product = db.query(Product).filter(Product.id == product_id).first()

    if product:
        product.pname = updated_product.pname
        product.price = updated_product.price
        product.qty = updated_product.qty
        product.meg = updated_product.meg

        db.commit()
        db.refresh(product)
        db.close()
        return product

    db.close()
    return None


# DELETE
def delete_product_service(product_id):
    db = SessionLocal()
    product = db.query(Product).filter(Product.id == product_id).first()

    if product:
        db.delete(product)
        db.commit()
        db.close()
        return True

    db.close()
    return False
```

---

# 🧱 Step 7: Keep Your Routes SAME

👉 No change needed in routes (good design 👍)

---

# 🧪 Step 8: Test in Swagger

Go:

```
http://127.0.0.1:8000/docs
```

Test:

* POST → create product
* GET → fetch
* PUT → update
* DELETE → delete

---

# 🧠 Big Concept (VERY IMPORTANT)

## Before:

```python
product_DB = []
```

## Now:

```python
db.query(Product)
```

👉 You moved from:

* ❌ Temporary memory
* ✅ Real database

---

# 🔥 Real-World Impact

Now your project:

* ✅ Persistent data
* ✅ Backend architecture
* ✅ Industry-ready base
* ✅ Portfolio-worthy 💼

---

# 💬 Quick Check

👉 What does this do?

```python
db.add(new_product)
db.commit()
```

---

# 🚀 Tomorrow (Day 8)

👉 **Pydantic + SQLAlchemy together (Best Practice)**

* Separate request & DB models
* Clean architecture

---

# 🎯 Your Task

✔ Implement DB
✔ Run server
✔ Test CRUD

---

Reply:

* ✅ “Working with DB”
* ❌ Error → I’ll debug instantly




----Detail Understanding----

Nice — this is exactly where things *click* for backend development. I’ll explain your code **end-to-end in very simple language**, like you're building it from scratch 👇

---

# 🧠 Big Picture (Why are we doing this?)

Before:

```python
product_DB = []
```

👉 Data stored in **RAM (temporary)**
👉 Server restart = data gone ❌

Now:
👉 We use **SQLite database**
👉 Data stored in a **file (`products.db`)**
👉 Restart server → data still there ✅

---

# 🧱 Step-by-Step Explanation

---

# 🔹 1. `database/db.py` → DB Setup

```python
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker, declarative_base
```

👉 Import tools to talk to database

---

### ✅ DATABASE URL

```python
DATABASE_URL = "sqlite:///./products.db"
```

👉 Means:

* Use **SQLite**
* Create file → `products.db`

---

### ✅ Engine (Connection Creator)

```python
engine = create_engine(DATABASE_URL, connect_args={"check_same_thread": False})
```

👉 Think:

> Engine = "Connection factory"

Why `check_same_thread=False`?
👉 SQLite normally allows **1 thread only**
👉 FastAPI uses multiple → so we disable restriction

---

### ✅ Session (Actual DB usage)

```python
SessionLocal = sessionmaker(bind=engine)
```

👉 Think:

> Session = "conversation with DB"

Every time:

```python
db = SessionLocal()
```

👉 You open a connection

---

### ✅ Base (Model Parent)

```python
Base = declarative_base()
```

👉 This is like:

> “All tables will inherit from this”

---

# 🔹 2. `models/product_model.py` → Table Structure

```python
class Product(Base):
```

👉 This means:

> This class = a database table

---

### ✅ Table Name

```python
__tablename__ = "products"
```

👉 Table name in DB

---

### ✅ Columns

```python
id = Column(Integer, primary_key=True, index=True)
pname = Column(String)
price = Column(Integer)
qty = Column(Integer)
meg = Column(String)
```

👉 This defines:

| Column | Type |
| ------ | ---- |
| id     | int  |
| pname  | text |
| price  | int  |
| qty    | int  |
| meg    | text |

---

👉 Important:

> This is NOT API request model
> This is DB table structure

---

# 🔹 3. `main.py` → Create Table

```python
Base.metadata.create_all(bind=engine)
```

👉 This line:

> “Create all tables if they don’t exist”

Without this:
❌ No table created
❌ DB operations fail

---

# 🔹 4. Service Layer (CORE LOGIC)

Now the real magic 👇

---

## ✅ CREATE

```python
db = SessionLocal()
```

👉 Open DB connection

---

```python
new_product = Product(...)
```

👉 Create object (not saved yet)

---

```python
db.add(new_product)
```

👉 Add to DB queue (not saved yet)

---

```python
db.commit()
```

👉 💥 Actually SAVE to database

---

```python
db.refresh(new_product)
```

👉 Reload data from DB (important if auto-generated fields exist)

---

```python
db.close()
```

👉 Close connection (VERY IMPORTANT)

---

### 🔥 Answer to your question:

```python
db.add(new_product)
db.commit()
```

👉 Means:

* `add` → prepare data
* `commit` → permanently save to DB

---

## ✅ READ ALL

```python
products = db.query(Product).all()
```

👉 Means:

> "Give me ALL rows from Product table"

---

## ✅ READ ONE

```python
product = db.query(Product).filter(Product.id == product_id).first()
```

👉 Means:

> "Find product where id = given id"

---

## ✅ UPDATE

```python
product = db.query(Product).filter(Product.id == product_id).first()
```

👉 Get existing product

---

```python
product.pname = updated_product.pname
```

👉 Modify fields

---

```python
db.commit()
```

👉 Save updated values

---

## ✅ DELETE

```python
db.delete(product)
db.commit()
```

👉 Remove from DB permanently

---

# 🔥 WHY SERVICE LAYER?

👉 You might think:
“Why not write DB code in routes?”

Because:

| Without Service | With Service |
| --------------- | ------------ |
| messy           | clean        |
| hard to scale   | scalable     |
| logic mixed     | separated    |

👉 Industry always uses:

> Routes → Service → DB

---

# 🧠 Flow of Your App

```
Client → Route → Service → DB
                     ↓
                  SQLite
```

---

# ⚡ SUPER SIMPLE ANALOGY

Think of DB like a **bank 🏦**

| Code    | Meaning           |
| ------- | ----------------- |
| engine  | bank building     |
| session | your visit        |
| add     | fill deposit slip |
| commit  | money deposited   |
| query   | check balance     |
| delete  | withdraw          |

---

# 🚀 Final Understanding

Now you are doing:

✅ Real data storage
✅ Industry-level architecture
✅ Proper DB handling
✅ Scalable backend

--


Got it 👍 — let’s make this **super clear with a simple flow diagram + explanation**

---

# 🔁 🔥 Full Backend Flow (End-to-End)

-------Client-----------
Client (Postman / Browser / Frontend)
                ↓
        FastAPI Route
                ↓
        Service Layer (Business Logic)
                ↓
        Database Session (SQLAlchemy)
                ↓
        SQLite Database (products.db file)
                ↓
        Response goes back ↑
```

---

# 🧠 Step-by-Step Flow (Real Example: CREATE Product)

### 1️⃣ Client Request

```
POST /products
{
  "id": 1,
  "pname": "Laptop",
  "price": 50000,
  "qty": 2,
  "meg": "Good"
}
```

👉 User sends data

---

### 2️⃣ Route Layer

```python
@router.post("/products")
def create_product(product: ProductSchema):
    return create_product_service(product)
```

👉 Route:

* receives request
* sends data to service

---

### 3️⃣ Service Layer (MAIN LOGIC)

```python
db = SessionLocal()
```

👉 Open DB connection

---

```python
db.add(new_product)
```

👉 Prepare to insert

---

```python
db.commit()
```

👉 💥 Save into database

---

```python
db.close()
```

👉 Close connection

---

### 4️⃣ Database (SQLite)

```
products.db

Table: products
------------------------
| id | pname | price |
------------------------
| 1  | Laptop | 50000 |
```

👉 Data stored permanently

---

### 5️⃣ Response Back

```
{
  "id": 1,
  "pname": "Laptop",
  "price": 50000,
  "qty": 2,
  "meg": "Good"
}
```

👉 Sent back to client

---

# 🔄 CRUD Flow (Quick View)

## ✅ CREATE

```
POST → Route → Service → add() → commit() → DB
```

## ✅ READ

```
GET → Route → Service → query() → DB → return data
```

## ✅ UPDATE

```
PUT → Route → Service → find → update → commit → DB
```

## ✅ DELETE

```
DELETE → Route → Service → find → delete → commit → DB
```

---

# 🎯 Ultra Simple Visual (Memory Trick)

```
User
 ↓
Route  (entry point)
 ↓
Service (brain 🧠)
 ↓
DB Session (connection 🔌)
 ↓
Database (storage 💾)
```

---

# ⚡ One-Line Understanding

👉 Route = receives request
👉 Service = does logic
👉 DB = stores data

---



